# Tests

Automated tests for Ananas belong here.

Every behavior-changing feature should arrive with appropriate tests. Agent/model benchmarks are kept separately under `benchmarks/` so product correctness tests and model evaluation remain distinct.
